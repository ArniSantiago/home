# Arni Santiago — complete GitHub Pages package

This package contains the two standalone sites, all image/PDF assets, the GitHub Pages route fixes, the portfolio lightbox module, the design-site 16px readability stylesheet, and the centered long Tools pill revision.

Repository root: `index.html`, `arni-design/`, `arni-portfolio/`, `.nojekyll`.

URLs:
- https://arnisantiago.github.io/home/
- https://arnisantiago.github.io/home/arni-design/
- https://arnisantiago.github.io/home/arni-portfolio/

Use GitHub Desktop: clone `ArniSantiago/home`, preserve the hidden `.git` folder, replace the repository contents with this package contents, commit to `main`, and push origin. Use Pages source `main` and `/(root)`.
